import XCTest
@testable import Advent

final class Day13Test: XCTestCase, IBaseTest {
    let dataTest = """
    """

    func testPart1() throws {
        check(testValue: .unowned, day13part1)
    }

    func testPart2() throws {
        check(testValue: .unowned, day13part2)
    }
}
