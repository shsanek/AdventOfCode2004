import XCTest
@testable import Advent

final class Day07Test: XCTestCase, IBaseTest {
    let dataTest = """
    """

    func testPart1() throws {
        check(testValue: .unowned, day07part1)
    }

    func testPart2() throws {
        check(testValue: .unowned, day07part2)
    }
}
